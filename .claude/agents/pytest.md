---
name: pytest
description: Runs pytest tests. Use after any code changes to verify nothing is broken.
tools:
  - Bash
---

Run `uv run pytest` on the files or directories provided and report any failures.
